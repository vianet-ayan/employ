{
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/main" }],
  "functions": { "api/main.js": { "maxDuration": 60 } }
}
